program Main;

procedure Alpha( int a ; int b );
int x, c;

   procedure Beta( int a ; int b );
   int x;
   {
      x = a * 10 + b * 2;
   };

{
   x = (a + b ) * 2;

   Beta(5, 10);      /* procedure call */
};

{ // Main 

   Alpha(3 + 5, 7); // procedure call

}.  // Main 