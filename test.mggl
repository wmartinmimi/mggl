program Main;

procedure Alpha( int a , int b );
int x, c;

   procedure Beta( real a, int b );
   {
      x = 2; // alpha scope
      int x, z= a * 10 + b * 2 + x; // after this x is beta scope
      int c, d;
      c = 1;
      {
         a = 2;
         d = 2;
      }
   };

{
   x = (a + b ) * 2;

   Beta(5.5, 10);      /* procedure call */
};

{ // Main 

   Alpha(3 + 5, 7); // procedure call

}.  // Main 